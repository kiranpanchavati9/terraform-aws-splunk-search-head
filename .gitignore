# Terraform
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log

# tfvars are committed in this repo — keep secrets out of them
# Use AWS Secrets Manager or SSM for passwords and licence keys
*.tfvars.json

# CLI config
.terraformrc
terraform.rc

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# OS
.DS_Store